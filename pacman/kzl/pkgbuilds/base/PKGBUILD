# Maintainer: Zelun Kong <zelun.kong@outlook.com>

pkgname=base
pkgver=20210810
pkgrel=1
epoch=1
pkgdesc="Minimal package set to define a basic KZL Linux installation"
arch=('any')
license=('KZL')
depends=(
  filesystem
  linux-api-headers 
  zlib
  glibc binutils gcc libtool pkgconf
  ncurses readline bash
  bzip2 xz gzip tar
  grep sed gawk m4 bison flex
  file which findutils
  shadow coreutils util-linux e2fsprogs procps-ng psmisc
  perl
  dbus
  iproute2 iputils
  systemd
  pciutils
  zstd pacman
  autoconf automake
  patch
  make
  python
  cmake
  git
  sudo
  gettext texinfo man-db
)
optdepends=("linux: bare metal support")
