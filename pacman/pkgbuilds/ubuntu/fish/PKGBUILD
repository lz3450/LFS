# Maintainer: Zelun Kong <zelun.kong@outlook.com>

pkgname=fish
pkgver=4.0.8
pkgrel=1
epoch=
pkgdesc="A smart and user-friendly command line shell"
arch=('x86_64')
url="https://fishshell.com/"
license=('KZL')
backup=(
  etc/fish/config.fish
)
install=$pkgname.install
source=(
  git+https://github.com/fish-shell/fish-shell.git#tag=$pkgver
)
sha256sums=('330f5b2587f1a1edd2b2ac1e9f6e51a1f9541203e5c82224a99a3942722bb499')

build() {
  cd fish-shell

  msg2 "Configuring..."
  cmake -LAH -S . -B build \
    -D CMAKE_BUILD_TYPE=Release \
    -D CMAKE_INSTALL_PREFIX="/$PREFIX" \
    -D CMAKE_C_FLAGS="$CFLAGS $CPPFLAGS" \
    -D CMAKE_CXX_FLAGS="$CXXFLAGS $CPPFLAGS" \
    -D BUILD_DOCS=OFF

  msg2 "Compiling..."
  cmake --build build
}

check() {
  cd fish-shell

  cmake --build build --target test
}

package() {
  cd fish-shell

  DESTDIR="$pkgdir" cmake --build build --target install
}
