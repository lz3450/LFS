base
    linux-api-headers
    glibc (ld.so.conf)
        tzdata
    gcc
        binutils mpc
    coreutils
        acl attr gmp libcap openssl
    util-linux (rfkill, uuidd.socket)
        libcap-ng pam shadow