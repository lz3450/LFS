# Maintainer: Zelun Kong <zelun.kong@outlook.com>

pkgname=linux-rpi
pkgver=5.4
pkgrel=1
pkgdesc="LTS Linux Kernel (Raspberry Pi)"
arch=('aarch64')
url="https://www.raspberrypi.org/documentation/linux/kernel/building.md"
makedepends=(bc)
options=(!strip)
source=(git+https://github.com/raspberrypi/linux#branch=rpi-5.4.y
        git+https://github.com/raspberrypi/firmware.git)
sha256sums=('SKIP'
            'SKIP')

export KBUILD_BUILD_HOST=kzl-linux
export KBUILD_BUILD_USER=kzl
export KBUILD_BUILD_TIMESTAMP="$(date -Ru${SOURCE_DATE_EPOCH:+d @$SOURCE_DATE_EPOCH})"
export KERNEL=kernel8

prepare() {
  cd linux

  make bcm2711_defconfig
  make nconfig
}

build() {
  cd linux

  make Image modules dtbs
}

package() {
  cd linux

  make INSTALL_MOD_PATH="$pkgdir/usr" modules_install

  install -Dm755 arch/arm64/boot/Image "$pkgdir"/boot/$KERNEL.img
  install -Dm755 arch/arm64/boot/dts/overlays/*.dtb* -t "$pkgdir"/boot/overlays/
  install -Dm755 arch/arm64/boot/dts/overlays/README -t "$pkgdir"/boot/overlays/
}
