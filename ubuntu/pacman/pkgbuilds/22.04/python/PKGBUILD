# Maintainer: Zelun Kong <zelun.kong@outlook.com>

pkgname=python
pkgver=3.10.17
pkgrel=2
epoch=
pkgdesc="Next generation of the python high-level scripting language"
arch=('x86_64')
url="https://www.python.org/"
license=('KZL')
source=(git+https://github.com/python/cpython.git#tag=v$pkgver)
sha256sums=('847fa353e2ffa525a40c1d85ebe55176899e5229e7498681a22e225af3889b15')

build() {
  cd cpython

  CFLAGS="${CFLAGS/-O2/-O3}"

  msg2 "Configuring..."
  ./configure \
    --prefix="/$PREFIX" \
    --enable-shared=yes \
    --enable-optimizations \
    --enable-loadable-sqlite-extensions=yes \
    --enable-ipv6=yes \
    --enable-big-digits=30 \
    --with-lto \
    --with-tzpath=/usr/share/zoneinfo \
    --with-system-expat=no \
    --with-system-ffi=no \
    --with-system-libmpdec=no \
    --with-dbmliborder=gdbm:ndbm \
    --with-readline=editline \
    --with-ensurepip=no

  msg2 "Compiling..."
  make
}

package() {
  cd cpython

  make DESTDIR="$pkgdir" install

  rm -vr "$pkgdir/$PREFIX/lib/python${pkgver%.*}"/{test,idlelib/idle_test}
}
