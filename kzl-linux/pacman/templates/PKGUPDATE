new_version=$(curl -L -A 'Mozilla/5.0' --stderr - )

pre_update() {

}

post_update() {
    makepkg -o
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > $PKGBUILDDIR/help.txt
    meson configure | tee $PKGBUILDDIR/help.txt
}
