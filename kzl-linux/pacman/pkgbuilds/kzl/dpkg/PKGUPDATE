new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://git.dpkg.org/git/dpkg/dpkg.git | grep -Po '(?<=/tag/\?h=)\d\.\d\d\.\d+' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
