new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://github.com/shadow-maint/shadow/tags | grep -oP '(?<=shadow/releases/tag/)\d\.\d{2}(\.\d)?' | head -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
