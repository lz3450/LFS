new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://www.rust-lang.org/ | grep -Po '(?<=Version )\d\.\d\d\.\d')

post_update() {
    cd "$BUILDDIR/$pkgname/src/rustc-$pkgver-src"
    ./configure --help > "$PKGBUILDDIR"/help.txt
    cat config.toml > "$PKGBUILDDIR"/config.toml
}
