new_version=$(git ls-remote --tags https://github.com/archlinux/arch-install-scripts.git | grep -Po '(?<=refs/tags/v)\d\d' | sort -V | tail -n1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    cat Makefile > "$PKGBUILDDIR"/help.txt
}
