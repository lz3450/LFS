new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://github.com/archlinux/arch-install-scripts/tags | grep -Po '(?<=/arch-install-scripts/releases/tag/v)\d\d' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
}
