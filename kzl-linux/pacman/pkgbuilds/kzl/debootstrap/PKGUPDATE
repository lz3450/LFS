new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://salsa.debian.org/installer-team/debootstrap/-/tags | grep -Po '(?<=/debootstrap/-/tags/)\d\.\d\.\d\d\d(\+nmu\d)?' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
}
