new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://people.redhat.com/sgrubb/audit/ | grep -Po '(?<=The latest stable release is )\d\.\d')

post_update() {
    cd "$BUILDDIR/$pkgname/src/audit-userspace"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
