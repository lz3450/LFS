new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://download.gnome.org/sources/libxml2/cache.json | grep -Po '(?<=LATEST-IS-)\d\.\d+.\d+' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}