new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://freetype.org/ | grep -Po '(?<=FreeType )\d\.\d\d\.\d' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/freetype"
    meson configure > $PKGBUILDDIR/help.txt
}
