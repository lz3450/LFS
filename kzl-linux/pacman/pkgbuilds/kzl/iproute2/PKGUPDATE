new_version=$(curl -L -A 'Mozilla/5.0' --stderr - $url | grep -Po '(?<=v)\d\.\d\.\d' | sort -V | tail -1)

post_update() {
    makepkg -o
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > $PKGBUILDDIR/help.txt
}
