new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://www.nasm.us/ | grep -A 2 "Stable" | grep -oP '(?<=/nasm/releasebuilds/)\d\.\d\d\.\d\d')

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
