new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://www.gnu.org/software/libunistring/ | grep -Po '(?<=libunistring-)\d\.\d' | head -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
