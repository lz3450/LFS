new_version=$(curl -L --stderr - https://www.gnu.org/software/libunistring/ | grep -Po '(?<=libunistring-)\d\.\d' | head -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
