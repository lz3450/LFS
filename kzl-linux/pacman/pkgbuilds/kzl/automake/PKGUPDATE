new_version=$(curl -L --stderr - https://ftp.gnu.org/gnu/automake/ | grep -Po '(?<=automake-)\d\.\d{2}\.\d' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
