new_version=$(curl -L --stderr - https://ftp.gnu.org/gnu/autoconf/ | grep -Po '(?<=autoconf-)\d\.\d{2}' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
