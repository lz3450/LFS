new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://gitlab.archlinux.org/pacman/pacman/-/tags | grep -Po '(?<=/pacman/-/tags/v)\d\.\d\.\d' | head -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    meson configure > "$PKGBUILDDIR"/help.txt
}
