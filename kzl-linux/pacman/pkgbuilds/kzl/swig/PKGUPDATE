new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://www.swig.org/ | grep -Po '(?<=SWIG-)\d\.\d\.\d' | head -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
