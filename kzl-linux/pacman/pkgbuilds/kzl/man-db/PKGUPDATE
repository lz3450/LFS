new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://gitlab.com/man-db/man-db/-/tags | grep -Po '(?<=/man-db/-/tags/)\d\.\d\d\.\d' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > "$PKGBUILDDIR"/help.txt
}
