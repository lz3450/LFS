new_version=$(curl -L -A 'Mozilla/5.0' --stderr - https://github.com/tmux/tmux/tags | grep -Po '(?<=tmux/releases/tag/)\d\.\d[a-z]' | sort -V | tail -1)

post_update() {
    cd "$BUILDDIR/$pkgname/src/$pkgname"
    ./configure --help > $PKGBUILDDIR/help.txt
}
